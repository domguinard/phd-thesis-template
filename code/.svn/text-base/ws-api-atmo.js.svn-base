function callback(event) {
  // Do something with the message
}
var location = "<t-Pusher URI>/topic";
$.atmosphere.subscribe(location, callback, $.atmosphere.request = {
            transport: 'websocket'
});
